spring:
  application:
    name: authentification_service

  datasource:
    url: jdbc:postgresql://localhost:5432/Auth-DB
    username: postgres
    password: tajidi180903
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      enabled: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/vintagly
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka