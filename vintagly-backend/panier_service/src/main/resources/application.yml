spring:
  application:
    name: grp8_panier_service

  datasource:
    url: jdbc:postgresql://148.230.115.143:5432/gr8_panier_db
    username: gr8_panier_user
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082